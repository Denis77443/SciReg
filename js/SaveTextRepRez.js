function Auto_Save_Text(){
    alert('dfdfd');
    /*        var text_report = $('textarea[name="text_report"]').val();
            var articles_in_russ = $('textarea[name="articles_in_russ"]').val();
            var articles_in_foreign = $('textarea[name="articles_in_foreign"]').val();
            var monograph = $('textarea[name="monograph"]').val();
            var reports_at_conf = $('textarea[name="reports_at_conf"]').val();
            var lecture_course = $('textarea[name="lecture_course"]').val();
            var patents = $('textarea[name="patents"]').val();
            var leadership = $('textarea[name="leadership"]').val();
            var other = $('textarea[name="other"]').val();
            var user_id = $('textarea[name="text_report"]')[0].id;
            
           // var lines = text_task.split(/\r|\r\n|\n/);
            //var count = lines.length;
            //console.log(count);
            //document.getElementById(user_id).rows = count;
            
            $.ajax({
                            type: "POST",
                            //url: "../Adm/ajax.save_textarea.php",
                            url: "index.php?url=ajax&param=save_report",
                            data: { action: 'Auto_Save_work',  
                                    user_id: user_id, 
                                    text_report: text_report, 
                                    articles_in_russ:articles_in_russ, 
                                    articles_in_foreign:articles_in_foreign, 
                                    monograph:monograph, 
                                    reports_at_conf:reports_at_conf, 
                                    lecture_course:lecture_course, 
                                    patents:patents, 
                                    leadership:leadership, 
                                    other:other },
                            cache: false,
                            success: function(responce){ $('div[id="textarea"]').html(responce); }
                    });
            */
        }
